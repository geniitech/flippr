%h1 Listing snapshots

%table
  %thead
    %tr
      %th Data
      %th Page
      %th
      %th
      %th

  %tbody
    - @snapshots.each do |snapshot|
      %tr
        %td= snapshot.data
        %td= snapshot.page_id
        %td= link_to 'Show', snapshot
        %td= link_to 'Edit', edit_snapshot_path(snapshot)
        %td= link_to 'Destroy', snapshot, :method => :delete, :data => { :confirm => 'Are you sure?' }

%br

= link_to 'New Snapshot', new_snapshot_path
